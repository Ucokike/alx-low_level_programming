#include "main.h"

/**
 * print_numbers -this program checks for a digit (0 through 9).
 * Return: Always 0. (success)
 */

void print_numbers(void)

{
	int c;

	for (c = 48; c < 58; c++)
	{
	_putchar(c);
	}
	_putchar('\n');
}
