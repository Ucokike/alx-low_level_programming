#include"main.h"
#include <stdio.h>

/**
 * mul -this multiplies two integers
 * @a: the first input
 * @b: the second input
 * Return: product of a & b
 */

int mul(int a, int b)

{
	int product = a * b;

	return (product);
}
